@model ICollection<MoviesRoom.ViewModels.Film.FilmViewModel>

@{
    ViewBag.Title = "Home Page";
}

@using (Ajax.BeginForm("Index", new AjaxOptions
{
    UpdateTargetId = "films",
    InsertionMode = InsertionMode.Replace
}))
{
    <input type="text" name="query" placeholder="Type a film name" />
    <input type="submit" value="Search &raquo;" class="btn btn-primary" />
}

<div id="films" class="row">
    @Html.Partial("_FilmPartial", Model)
</div>